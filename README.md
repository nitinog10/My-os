# nitinog10/My-os — Documentation

> Auto-generated by [DocuVerse](https://docuverse.ai)

## Overview

The `repo_254d7fa0211e` repository contains the source code for a browser-based operating system simulation called WebOS. This project aims to provide a fully functional OS experience within a web browser, featuring a desktop shell, virtual file system, terminal, code editor, file explorer, notes app, browser, and an AI assistant. The system uses AWS Cognito for authentication and DynamoDB for data persistence.

The tech stack includes TypeScript for the frontend and backend, with React for the UI components. The AI service is built using Python. The project is structured into several directories: `ai-service` for the AI component, `backend` for the server-side logic, and `frontend` for the client-side application. Additional documentation is provided in markdown files at the root and within the `.kiro` directory, which contains detailed specifications and architectural guidelines.

Users of this project would likely be developers interested in creating or exploring browser-based operating systems, or those looking to implement similar complex web applications with advanced features like AI integration and cloud persistence. The repository is well-documented, with guides for setup, contribution, and development practices to ensure maintainability and scalability.

## Architecture

```markdown
## Code Organization

The codebase is organized into several key directories, each serving a distinct purpose:

- **`ai-service/`**: Contains the AI service implementation, including environment configurations, the main service script, and dependencies.
- **`backend/`**: Houses the backend logic, with subdirectories for middleware, routes, services, and types. Key files include `server.ts` for setting up the server and `dynamoService.ts` for interacting with DynamoDB.
- **`frontend/`**: Contains the frontend code, organized into components, stores, types, and utilities. The `src/` directory includes the main application files like `App.tsx` and `main.tsx`.
- **`.kiro/`**: Holds architectural and specification documents, including detailed specs for each feature and overall architectural guidelines.

## Data Flow

Data flows through the system in the following manner:

1. **User Interaction**: Users interact with the frontend, triggering actions such as opening applications or editing files.
2. **Frontend to Backend**: These actions are sent to the backend via API requests. For example, file operations are handled by the `files.ts` route.
3. **Backend Processing**: The backend processes these requests, often involving services like `dynamoService.ts` for database interactions.
4. **Database Interaction**: Data is persisted or retrieved from DynamoDB, ensuring state is maintained across sessions.
5. **Response to Frontend**: The backend sends responses back to the frontend, updating the UI accordingly.

## Key Design Patterns

- **MVC (Model-View-Controller)**: The frontend uses a variation of MVC, where components act as views, stores as models, and actions as controllers.
- **Service Layer**: The backend employs a service layer (`services/`) to handle business logic, keeping the routes clean and focused on request handling.
- **Dependency Injection**: Utilized in the backend to manage dependencies, making the code more testable and maintainable.

## Main Entry Points

- **Frontend**: The main entry point is `main.tsx`, which bootstraps the React application.
- **Backend**: The primary entry point is `server.ts`, which sets up the Express server and defines middleware and routes.
- **AI Service**: The main entry point is `main.py`, which initializes the AI service and defines its functionality.
```

## Folder Structure

```
├── ARCHITECTURE.md
├── CHANGELOG.md
├── CONTRIBUTING.md
├── DEVELOPER_GUIDE.md
├── FEATURES.md
├── FILE_INDEX.md
├── LICENSE
├── PROJECT_STATUS.md
├── QUICKSTART.md
├── README.md
├── SETUP.md
├── SUMMARY.md
├── package.json
├── .kiro/
│   ├── specs/
│   │   ├── 01-desktop-shell.md
│   │   ├── 02-virtual-filesystem.md
│   │   ├── 03-terminal-app.md
│   │   ├── 04-code-editor-app.md
│   │   ├── 05-file-explorer-app.md
│   │   ├── 06-notes-app.md
│   │   ├── 07-browser-app.md
│   │   ├── 08-auth-cognito.md
│   │   ├── 09-dynamo-persistence.md
│   │   ├── 10-ai-assistant.md
│   │   └── 11-advanced-features.md
│   └── steering/
│       ├── architecture.md
│       ├── dynamo-schema.md
│       ├── product.md
│       └── tech-stack.md
├── ai-service/
│   ├── .env.example
│   ├── Procfile
│   ├── README.md
│   ├── main.py
│   └── requirements.txt
├── backend/
│   ├── .env.example
│   ├── Procfile
│   ├── package.json
│   ├── tsconfig.json
│   └── src/
│       ├── server.ts
│       ├── middleware/
│       │   └── authMiddleware.ts
│       ├── routes/
│       │   ├── ai.ts
│       │   ├── auth.ts
│       │   └── files.ts
│       ├── services/
│       │   └── dynamoService.ts
│       └── types/
│           └── index.ts
└── frontend/
    ├── .env.example
    ├── index.html
    ├── package.json
    ├── postcss.config.js
    ├── tailwind.config.js
    ├── tsconfig.json
    ├── vercel.json
    ├── vite.config.ts
    ├── public/
    │   ├── favicon.svg
    │   └── icons.svg
    └── src/
        ├── App.tsx
        ├── main.tsx
        ├── style.css
        ├── assets/
        │   ├── hero.png
        │   ├── typescript.svg
        │   └── vite.svg
        ├── components/
        │   ├── AppLauncher.tsx
        │   ├── Desktop.tsx
        │   ├── LoginScreen.tsx
        │   ├── Taskbar.tsx
        │   ├── WindowFrame.tsx
        │   ├── WindowManager.tsx
        │   ├── CodeEditor/
        │   │   └── index.tsx
        │   ├── FileExplorer/
        │   │   ├── TreeNode.tsx
        │   │   └── index.tsx
        │   ├── Notes/
        │   │   └── index.tsx
        │   └── Terminal/
        │       └── index.tsx
        ├── stores/
        │   ├── authStore.ts
        │   ├── editorStore.ts
        │   ├── fileSystemStore.ts
        │   ├── themeStore.ts
        │   └── windowStore.ts
        ├── types/
        │   └── index.ts
        └── utils/
            ├── appRegistry.ts
            └── commandExecutor.ts
```

## Dependencies

```markdown
## Dependencies

### Major Libraries

- **React**: A JavaScript library for building user interfaces. Used in the frontend module.
  - Version: Specified in `frontend/package.json`
  - Purpose: Building the user interface for the web-based operating system.

- **TypeScript**: A typed superset of JavaScript that compiles to plain JavaScript. Used in both frontend and backend modules.
  - Version: Specified in `frontend/package.json` and `backend/package.json`
  - Purpose: Adding static types to JavaScript for better development experience and error checking.

- **AWS SDK**: A set of libraries and tools for interacting with AWS services.
  - Version: Specified in `backend/package.json`
  - Purpose: Integrating AWS services into the backend.

### Version Constraints

- The versions of all dependencies are specified in the respective `package.json` files of the `frontend`, `backend`, and `ai-service` modules.

### Development vs Production Dependencies

- **Development Dependencies**:
  - Used during the development phase to facilitate the development process.
  - Examples include `webpack`, `babel`, and `eslint`.

- **Production Dependencies**:
  - Used in the final build and deployment of the application.
  - Examples include `express` for the backend server, and `react-dom` for rendering the React components in the browser.
```

## File Documentation

### ARCHITECTURE.md

#### Overview

# ARCHITECTURE.md

#### Module Overview

This file provides an overview of the WebOS architecture, detailing the system components, data flow, security measures, performance optimizations, scalability, and future enhancements.

#### Dependencies

- **React**: Frontend framework for building UI components.
- **Zustand**: State management library for global state.
- **Express**: Backend framework for handling HTTP requests.
- **AWS SDK**: Library for interacting with AWS services.
- **DynamoDB**: NoSQL database service for storing user data.
- **Cognito**: AWS service for user authentication and authorization.
- **FastAPI**: Python framework for building the AI service.
- **Anthropic Claude API**: External API for AI chat functionality.
- **SSE**: Server-Sent Events for real-time communication.

#### Classes

| Class           | Purpose                              | Key Methods                  |
|-----------------|--------------------------------------|------------------------------|
| `App`           | Root component of the React frontend | `render()`                   |
| `WindowManager` | Manages window lifecycle and layout  | `openWindow()`, `closeWindow()` |
| `Taskbar`       | Displays active applications         | `render()`                   |

#### Functions

| Function        | Parameters           | Returns     | Description                                  |
|-----------------|----------------------|-------------|----------------------------------------------|
| `loginUser`     | `email`, `password`  | `Promise`   | Authenticates user and stores JWT token      |
| `loadFileSystem`| None                 | `Promise`   | Fetches user's file system from DynamoDB     |
| `createFile`    | `path`, `content`    | `Promise`   | Creates a new file or folder in DynamoDB     |
| `updateFile`    | `path`, `content`    | `Promise`   | Updates the content of a file in DynamoDB    |
| `deleteFile`    | `path`               | `Promise`   | Deletes a file or folder from DynamoDB       |
| `aiChat`        | `message`            | `Promise`   | Sends a message to the AI service and streams response |

#### Configuration

- **Ports**:
  - Frontend: `3000`
  - Backend: `4000`
  - AI Service: `8000`

#### Constants

- **DynamoDB Table**: `webos-main`
- **Partition Key Prefix**: `user#`
- **Sort Key Prefix**: `FS#`

#### Notes

- **Security**: All data queries are scoped to the user ID extracted from the JWT token. There is no direct AWS calls from the frontend.
- **Performance**: The frontend uses code splitting, lazy loading, and virtual scrolling to optimize performance. The backend uses connection pooling and JWT verification caching.
- **Scalability**: The system is designed for horizontal scaling with stateless services and vertical scaling with AWS managed services.
- **Future Enhancements**: Planned features include real-time collaboration, file sharing, plugins, mobile support, offline mode, search, notifications, keyboard shortcuts, themes, and analytics.

### CHANGELOG.md

#### Overview

# CHANGELOG.md

This file documents all notable changes to the WebOS project, providing a clear history of updates and improvements.

#### Dependencies

There are no specific dependencies for this file, as it is a markdown document meant for human readers.

#### Notes

- Each release section follows the [Keep a Changelog](https://keepachangelog.com/en/1.0.0/) format.
- The `Version History` section provides a quick overview of major releases.
- The `Upgrade Guide` section helps users transition between versions.
- The `Known Issues` and `Coming Soon` sections highlight current limitations and future plans.

#### Sections

| Section | Purpose |
|---------|---------|
| Known Issues | Lists current limitations and bugs. |
| Coming Soon | Highlights features planned for future releases. |
| Future Releases | Provides a roadmap of planned features for upcoming versions. |
| Version History | Summarizes major releases and their dates. |
| Upgrade Guide | Offers guidance on upgrading from previous versions. |
| Breaking Changes | Lists any breaking changes introduced in the release. |
| Deprecations | Notes any features or APIs that are deprecated. |
| Security Updates | Details any security-related changes or patches. |
| Contributors | Acknowledges contributors to the project. |
| Feedback | Encourages user feedback and contributions. |
| License | Specifies the licensing terms for the project. |

#### Example Entry

```markdown

#### [1.0.0] - 2026-04-07

### Initial Release 🎉

#### Core Features
- Desktop environment with glassmorphism UI
- Window management system (drag, resize, minimize, maximize, close)
- Z-index management for window focus
- Taskbar with open windows and system tray
- App launcher with icon grid
- Smooth animations with Framer Motion
- Dark mode theme with custom wallpaper support

#### Authentication
- User signup with AWS Cognito
- User login with email/password
- JWT token management
- Protected routes and session persistence
- Auto-login on signup

#### File System
- Virtual file system with tree structure
- DynamoDB persistence per user
- Default file tree on first login
- File and folder CRUD operations
- Path resolution utilities

#### Apps

**Terminal**
- Command-line interface with history
- Commands: ls, cd, pwd, cat, echo, clear, help
- Colored output (command, output, error)
- Current working directory tracking

**Code Editor**
- Monaco Editor integration
- Multi-tab support
- Syntax highlighting (JS, TS, Python, JSON, Markdown)
- Auto-save with 2-second debounce
- Dirty state indicator

**File Explorer**
- Tree view of file system
- Expandable/collapsible folders
- File type icons
- Double-click to open files in editor

**Notes**
- Split-pane layout (editor + preview)
- Markdown editor with live preview
- Syntax highlighting in preview

#### Backend
- Express server with TypeScript
- JWT authentication middleware
- Auth routes (login, signup, me)
- File routes (CRUD operations)
- AI proxy route with SSE
- Health check endpoint

#### AI Service
- FastAPI server with Python
- Anthropic Claude integration
- Server-Sent Events (SSE) streaming
- Context-aware prompts

#### State Management
- Zustand stores for all state
- Window store (lifecycle, focus, z-index)
- File system store (tree, CRUD)
- Auth store (user, token, session)
- Editor store (tabs, active file)
- Theme store (mode, wallpaper, accent)

#### Developer Experience
- TypeScript strict mode
- Hot module replacement (HMR)
- Environment variable configuration
- Comprehensive documentation
- Setup guides and architecture docs

#### Documentation
- README.md - Project overview
- SETUP.md - Detailed setup instructions
- QUICKSTART.md - 5-minute quick start
- ARCHITECTURE.md - System design
- FEATURES.md - Feature checklist
- DEVELOPER_GUIDE.md - Development guide
- CONTRIBUTING.md - Contribution guidelines
- LICENSE - MIT License

#### Deployment
- Vercel configuration for frontend
- Railway/Heroku configuration for backend
- Environment variable templates
- Production deployment guides

### Known Issues
- Terminal mkdir, touch, rm commands not yet implemented
- AI assistant not integrated into terminal
- File Explorer context menu not implemented
- No keyboard shortcuts yet
- No settings panel yet

### Coming Soon
- Complete terminal commands
- AI assistant integration
- File Explorer context menu
- Keyboard shortcuts
- Settings panel
- Browser app
- Calculator app
```

#### Tips

- Always check the `Known Issues` section before reporting a bug.
- Review the `Coming Soon` section to see if a feature is planned for future releases.
- Use the `Upgrade Guide` to smoothly transition between versions.
- Contribute by opening issues, suggesting features, or submitting pull requests.

Feel free to reach out if you have any questions or need further clarification!

### CONTRIBUTING.md

#### Overview

# Module Overview

The `CONTRIBUTING.md` file provides comprehensive guidelines and instructions for contributing to the WebOS project. It outlines the code of conduct, contribution processes, project structure, development guidelines, and areas needing help. This document ensures that new contributors understand how to effectively participate in the project.

#### Dependencies

- None (This is a markdown file with no external dependencies)

#### Notes

- Contributors should familiarize themselves with the project structure and development guidelines before making contributions.
- Ensure that all contributions adhere to the code of conduct and follow the specified contribution processes.
- High-priority features should be addressed first, followed by medium and low-priority features.
- Documentation improvements are always welcome and can significantly enhance the project.
- Design contributions, such as UI/UX improvements and theme designs, are encouraged.
- Contributors will be recognized in `CONTRIBUTORS.md`, release notes, and documentation.
- All contributions are licensed under the MIT License.

### DEVELOPER_GUIDE.md

#### Overview

# DEVELOPER_GUIDE.md

#### Module Overview

This file provides a comprehensive guide for developers working on the WebOS project, covering everything from adding new apps and backend routes to performance optimization and security best practices.

#### Dependencies

- `React` and `TypeScript` for frontend development.
- `Zustand` for global state management.
- `Express` for backend routing.
- `Axios` for making API calls.
- `AWS SDK` for interacting with AWS services.
- `ESLint` and `Prettier` for code style and formatting.

#### Classes

None.

#### Functions

None.

#### Configuration

None.

#### Constants

None.

#### Notes

- Always run `npm test` before deploying to ensure all tests pass.
- Use `React.memo`, `useCallback`, and `useMemo` for performance optimization in React components.
- Follow security best practices to protect sensitive data and prevent common vulnerabilities.
- Refer to the `Resources` section for external documentation and libraries used in the project.

### FEATURES.md

#### Overview

# WebOS Features

#### Module Overview

This file documents the implemented, partially implemented, and planned features for WebOS. It serves as a roadmap for development and a reference for contributors.

#### Dependencies

None. This is a markdown file for documentation purposes.

#### Notes

- The feature completion percentages are estimates and subject to change.
- The roadmap is flexible and may be adjusted based on user feedback and development priorities.
- Contributions are welcome! Refer to the "Contributing" section for guidance.

### FILE_INDEX.md

#### Overview

# Module Overview

Complete index of all project files with descriptions.

#### Dependencies

- None

#### Notes

- This file is auto-generated. Do not edit manually.
- Refer to `README.md` for project overview.
- Use `SUMMARY.md` for complete implementation summary.

### LICENSE

#### Overview

# LICENSE

#### Module Overview

This file contains the MIT License for the project. It outlines the terms under which the software can be used, modified, and distributed.

#### Dependencies

None.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| N/A | N/A | N/A | No functions defined in this file. |

#### Notes

- This license allows for broad usage, modification, and distribution of the software.
- It mandates that the original copyright notice and this permission notice be included in all copies or substantial portions of the software.
- The software is provided "as is" without any warranties, and the authors are not liable for any damages arising from its use.

### PROJECT_STATUS.md

#### Overview

# Module Overview
This file provides a snapshot of the current status of the WebOS project, including overall progress, component status, feature checklist, milestone progress, development timeline, known issues, next sprint goals, code statistics, quality metrics, learning resources, contact information, achievements, work in progress, and future vision.

#### Dependencies

None. This file is a markdown document and does not rely on any external modules or libraries.

#### Configuration

None. This file does not contain any configuration settings.

#### Constants

None. This file does not define any constants.

#### Notes

- The progress bars and percentages are manually updated and should be checked for accuracy.
- The feature checklist, milestone progress, and development timeline are subject to change based on project priorities and resource availability.
- The known issues section is a living document and should be updated as issues are resolved or new ones are identified.
- The next sprint goals are tentative and may be adjusted based on team capacity and project needs.
- The code statistics and quality metrics are approximate and should be verified using appropriate tools.
- The learning resources section is a starting point for new contributors and should be expanded as needed.
- The contact and support information should be kept up-to-date to ensure effective communication within the team.
- The achievements section highlights key milestones reached in the project and should be updated as new features are completed.
- The work in progress section outlines current development efforts and should be updated as tasks are completed or new ones are started.
- The future vision section describes the long-term goals for the project and should be reviewed periodically to ensure alignment with team objectives.

### QUICKSTART.md

#### Overview

# WebOS Quick Start Guide

This guide helps you get WebOS up and running in under 5 minutes.

#### Prerequisites

- Node.js 18+
- Python 3.11+
- AWS Account (free tier works)
- Anthropic API key

#### Step 1: Clone and Install (2 minutes)

```bash
# Clone the repository
git clone <your-repo-url>
cd webos

# Install all dependencies
npm run install:all

# Install AI service dependencies
cd ai-service
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
cd..
```

#### Step 2: AWS Setup (2 minutes)

### DynamoDB

1. Go to AWS Console → DynamoDB
2. Create table: `webos-main`
3. Partition key: `PK` (String)
4. Sort key: `SK` (String)
5. Billing: On-demand

### Cognito

1. Go to AWS Console → Cognito
2. Create User Pool
3. Enable email sign-in
4. Create App Client (public, no secret)
5. Enable `ALLOW_USER_PASSWORD_AUTH`
6. Copy User Pool ID and Client ID

#### Step 3: Configure Environment (1 minute)

```bash
# Frontend
cp frontend/.env.example frontend/.env
# Edit: Add Cognito User Pool ID and Client ID

# Backend
cp backend/.env.example backend/.env
# Edit: Add AWS credentials and Cognito details

# AI Service
cp ai-service/.env.example ai-service/.env
# Edit: Add Anthropic API key
```

#### Step 4: Start Services (30 seconds)

Open 3 terminals:

**Terminal 1:**

```bash
npm run dev:frontend
```

**Terminal 2:**

```bash
npm run dev:backend
```

**Terminal 3:**

```bash
cd ai-service
source venv/bin/activate  # Windows: venv\Scripts\activate
python main.py
```

#### Step 5: Use WebOS! 🎉

1. Open `http://localhost:3000`
2. Sign up with email/password
3. Explore the desktop!

#### What to Try

### Terminal

```bash
ls                    # List files
cd documents          # Change directory
cat welcome.txt       # Read file
echo "Hello WebOS"    # Print text
help                  # Show commands
```

### Code Editor

1. Click 📝 icon
2. Open File Explorer (📁 icon)
3. Double-click `welcome.txt`
4. Edit and save (auto-saves in 2s)

### File Explorer

1. Click 📁 icon
2. Browse folders
3. Double-click files to open in editor

### Notes

1. Click 📋 icon
2. Write Markdown on left
3. See preview on right

#### Troubleshooting

### Frontend won't start

```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### Backend won't start

- Check AWS credentials
- Verify DynamoDB table exists
- Check Cognito configuration

### AI Service won't start

- Check Python version: `python --version`
- Verify virtual environment is activated
- Check Anthropic API key

### Can't login

- Verify Cognito User Pool exists
- Check App Client configuration
- Ensure `ALLOW_USER_PASSWORD_AUTH` is enabled

#### Next Steps

- Read `README.md` for full documentation
- Check `FEATURES.md` for available features
- See `DEVELOPER_GUIDE.md` to add features
- Review `ARCHITECTURE.md` for system design

#### Need Help?

- Check documentation files
- Open an issue on GitHub
- Join Discord community

#### Production Deployment

See `SETUP.md` for production deployment instructions to:

- Vercel (Frontend)
- Railway (Backend + AI Service)

Enjoy WebOS! 🚀

### README.md

#### Overview

# WebOS - Browser-Based Operating System

A fully functional operating system simulation running in your browser, built with React, TypeScript, and AWS services.

#### Module Overview

This `README.md` file provides an overview of the WebOS project, including its features, tech stack, project structure, setup instructions, environment variables, available terminal commands, development instructions, deployment options, architecture, license, and contribution guidelines.

#### Dependencies

- React 18 + Vite + TypeScript
- Tailwind CSS (styling)
- Framer Motion (animations)
- Zustand (state management)
- react-rnd (window management)
- Monaco Editor (code editing)
- Node.js + Express + TypeScript
- AWS SDK v3 (DynamoDB)
- AWS Cognito (authentication)
- Python 3.11 + FastAPI
- Anthropic Claude API
- Server-Sent Events (SSE) streaming

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `WindowManager` | Manages window state and interactions | `addWindow`, `removeWindow`, `updateWindow` |
| `FileExplorer` | Handles file system browsing and interactions | `navigate`, `openFile`, `createFile` |
| `Terminal` | Provides terminal interface and command handling | `executeCommand`, `renderOutput` |
| `CodeEditor` | Manages code editing functionality | `loadFile`, `saveFile`, `updateEditor` |
| `NotesApp` | Handles notes creation and editing | `createNote`, `editNote`, `deleteNote` |
| `AIAssistant` | Integrates AI functionality into the terminal | `sendPrompt`, `receiveResponse` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `setupAWS` | None | None | Initializes AWS SDK with credentials |
| `authenticateUser` | `username`, `password` | `Promise<User>` | Authenticates user via Cognito |
| `fetchFile` | `filePath` | `Promise<File>` | Retrieves file from DynamoDB |
| `saveFile` | `filePath`, `content` | `Promise<void>` | Saves file to DynamoDB |
| `executeTerminalCommand` | `command` | `Promise<string>` | Executes terminal command and returns output |

#### Configuration

### Environment Variables

#### Frontend (.env)
```
VITE_API_URL=http://localhost:4000
VITE_COGNITO_USER_POOL_ID=us-east-1_xxxxxxxxx
VITE_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
VITE_COGNITO_REGION=us-east-1
```

#### Backend (.env)
```
PORT=4000
AWS_REGION=us-east-1
DYNAMODB_TABLE=webos-main
COGNITO_USER_POOL_ID=us-east-1_xxxxxxxxx
COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
AI_SERVICE_URL=http://localhost:8000
```

#### AI Service (.env)
```
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
PORT=8000
```

#### Notes

- Ensure AWS credentials are correctly configured in `.env` files for both frontend and backend.
- The AI service requires a valid Anthropic API key in its `.env` file.
- All AWS operations are routed through the Express backend for security reasons.
- The file system state is managed by Zustand for runtime and DynamoDB for persistence.
- Each application component is a self-contained React component.
- Windows are managed centrally by the `WindowManager` class.
- The AI service runs as a separate FastAPI process.

### SETUP.md

#### Overview

# SETUP.md

#### Module Overview

This file provides a comprehensive guide for setting up the WebOS project, covering installation, configuration, and deployment steps. It's essential for developers to follow these instructions to ensure the project runs smoothly in both development and production environments.

#### Dependencies

- `npm`: Node Package Manager for installing JavaScript packages.
- `python`: Required for setting up the virtual environment and running the AI service.
- `pip`: Python package installer for managing dependencies in the AI service.

#### AWS Setup (Required)

### DynamoDB Table Setup

1. Go to AWS Console → DynamoDB
2. Create table:
   - Table name: `webos-main`
   - Partition key: `PK` (String)
   - Sort key: `SK` (String)
   - Billing mode: On-demand
3. Click "Create table"

### Cognito User Pool Setup

1. Go to AWS Console → Cognito
2. Create User Pool:
   - Sign-in options: Email
   - Password policy: Default
   - MFA: Optional (recommended: OFF for development)
   - User account recovery: Email only
   - Self-service sign-up: Enabled
   - Attribute verification: Email
3. Configure App Client:
   - App type: Public client
   - App client name: `webos-client`
   - Authentication flows: `ALLOW_USER_PASSWORD_AUTH`, `ALLOW_REFRESH_TOKEN_AUTH`
   - Don't generate client secret
4. Note your:
   - User Pool ID (e.g., `us-east-1_xxxxxxxxx`)
   - App Client ID (e.g., `xxxxxxxxxxxxxxxxxxxxxxxxxx`)

### IAM Permissions

Your AWS credentials need these permissions:
- `dynamodb:GetItem`
- `dynamodb:PutItem`
- `dynamodb:Query`
- `dynamodb:DeleteItem`
- `cognito-idp:InitiateAuth`
- `cognito-idp:SignUp`

#### OpenAI API Key

1. Go to https://platform.openai.com/
2. Create an account or sign in
3. Navigate to API Keys
4. Create a new API key
5. Copy the key (starts with `sk-`)
6. Add to `ai-service/.env`: `OPENAI_API_KEY=sk-...`

Note: OpenAI offers pay-as-you-go pricing. GPT-3.5-turbo is very affordable (~$0.0005 per 1K tokens).

#### Testing the Setup

### 1. Test Backend Health
```bash
curl http://localhost:4000/health
# Should return: {"status":"ok"}
```

### 2. Test AI Service Health
```bash
curl http://localhost:8000/health
# Should return: {"status":"ok"}
```

### 3. Test Frontend
- Open `http://localhost:3000`
- You should see the login screen
- Try signing up with a test account

#### Troubleshooting

### Frontend won't start
- Check Node.js version (18+)
- Delete `node_modules` and run `npm install` again
- Check for port conflicts on 3000

### Backend won't start
- Check AWS credentials are configured
- Verify DynamoDB table exists
- Check Cognito User Pool ID and Client ID
- Check for port conflicts on 4000

### AI Service won't start
- Check Python version (3.11+)
- Verify virtual environment is activated
- Check Anthropic API key is valid
- Check for port conflicts on 8000

### Authentication fails
- Verify Cognito User Pool configuration
- Check App Client has correct auth flows enabled
- Ensure no client secret is configured
- Check AWS region matches in all configs

### File system not loading
- Verify DynamoDB table exists
- Check AWS credentials have DynamoDB permissions
- Check table name matches in backend .env

#### Production Deployment

### Frontend (Vercel)
1. Push code to GitHub
2. Import project in Vercel
3. Set root directory to `frontend`
4. Add environment variables
5. Deploy

### Backend (Railway)
1. Create new project in Railway
2. Add Node.js service
3. Set root directory to `backend`
4. Add environment variables
5. Deploy

### AI Service (Railway)
1. Add Python service to same project
2. Set root directory to `ai-service`
3. Add environment variables
4. Deploy

### Update Frontend API URL
After deploying backend and AI service, update:
- `frontend/.env` → `VITE_API_URL` to backend URL
- `backend/.env` → `AI_SERVICE_URL` to AI service URL

#### Next Steps

- Customize the wallpaper and theme
- Add more terminal commands
- Implement file upload/download
- Add more apps (Browser, Calculator, etc.)
- Implement keyboard shortcuts
- Add notification system
- Implement search functionality

#### Notes

- Ensure all environment variables are correctly set in their respective `.env` files.
- Double-check AWS region settings across all services to avoid mismatches.
- Regularly update dependencies to avoid compatibility issues.

### SUMMARY.md

#### Overview

# Module Overview

This file provides a complete summary of the WebOS project, detailing the current implementation status, project structure, core features, technology stack, and future plans.

#### Dependencies

- **Markdown**: For formatting the document.

#### Configuration

- **Environment Variables**: Required for AWS setup and service configurations.

#### Notes

- **Project Status**: The project is currently 75% complete.
- **Immediate Next Steps**: Focus on completing terminal commands, adding File Explorer features, and integrating the AI assistant.
- **Long-term Goals**: Include real-time collaboration, mobile responsiveness, offline mode, and a plugin system.
- **Documentation**: Ensure to refer to `QUICKSTART.md` for detailed setup instructions and `FEATURES.md` for the complete feature list.

### package.json

#### Overview

# package.json

#### Module Overview

This file defines the project's dependencies, scripts, and metadata. It's essential for managing the project's environment and automating tasks like installation, development, and building.

#### Dependencies

This file does not directly import dependencies but lists scripts that rely on various packages installed in different directories.

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|------------|
| `install:all` | None | None | Installs dependencies in both frontend and backend directories. |
| `dev:frontend` | None | None | Runs the frontend development server. |
| `dev:backend` | None | None | Runs the backend development server. |
| `dev:ai` | None | None | Runs the AI service using Python. |
| `build:frontend` | None | None | Builds the frontend for production. |
| `build:backend` | None | None | Builds the backend for production. |

#### Configuration

| Key | Value | Description |
|-----|-------|-------------|
| `name` | "webos" | The name of the project. |
| `version` | "1.0.0" | The current version of the project. |
| `description` | "Browser-based Operating System" | A brief description of the project. |
| `keywords` | ["webos", "react", "typescript", "aws"] | Keywords associated with the project. |
| `author` | "" | The author of the project (currently empty). |
| `license` | "MIT" | The license under which the project is distributed. |

#### Notes

- Ensure you run `npm install` in the respective directories before using the scripts.
- The `dev:ai` script assumes a Python environment is set up in the `ai-service` directory.
- The `author` field is currently empty and should be filled with the appropriate name.

### .kiro/specs/01-desktop-shell.md

#### Overview

# Module Overview

This file defines the core components and logic for the desktop shell, which serves as the main operating system environment for our application. It includes the desktop canvas, window system, taskbar, and app launcher, all designed to provide a seamless user experience.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `react-rnd` | For draggable and resizable windows. |
| `Framer Motion` | For animations on window open/close actions. |
| `Zustand` | For state management of windows and themes. |
| `Tailwind CSS` | For styling, including the glassmorphism theme. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `WindowManager` | Manages rendering of all open windows. | `renderWindows()` |
| `WindowFrame` | Wraps and controls individual windows. | `handleDrag()`, `handleResize()` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `openWindow` | `windowId`, `title`, `content` | `void` | Opens a new window with specified properties. |
| `closeWindow` | `windowId` | `void` | Closes a window by its ID. |
| `minimizeWindow` | `windowId` | `void` | Minimizes a window by its ID. |
| `focusWindow` | `windowId` | `void` | Brings a window to the front by its ID. |

#### Configuration

| Configuration | Purpose |
| --- | --- |
| `themeStore` | Manages the application theme, including dark mode. |
| `windowStore` | Manages the state of all windows, including open/close actions. |

#### Notes

- Ensure all windows are correctly stacked by focus using the `focusWindow` method.
- The `WindowManager` component should render all windows from the Zustand store.
- The glassmorphism theme is applied via Tailwind CSS configurations in the `tailwind.config.js` file.
- Double-clicking on desktop icons should trigger the `openWindow` action from the `windowStore`.

### .kiro/specs/02-virtual-filesystem.md

#### Overview

# Module Overview

This file defines the in-memory virtual file system, which is synced to DynamoDB for persistence. It provides full CRUD operations for files and folders, with path-based addressing and synchronization to DynamoDB on every mutation.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Zustand` | State management for the in-memory file system. |
| `dynamoService` | Contains methods to interact with DynamoDB. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `FSNode` | TypeScript interface for file system nodes. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `createFile` | `path`, `content` | `FSNode` | Creates a new file at the specified path. |
| `createFolder` | `path` | `FSNode` | Creates a new folder at the specified path. |
| `deleteNode` | `path` | `boolean` | Deletes the node at the specified path. |
| `renameNode` | `oldPath`, `newPath` | `FSNode` | Renames the node from oldPath to newPath. |
| `moveNode` | `oldPath`, `newPath` | `FSNode` | Moves the node from oldPath to newPath. |
| `readFile` | `path` | `string` | Reads the content of the file at the specified path. |
| `writeFile` | `path`, `content` | `FSNode` | Writes the content to the file at the specified path. |

#### Configuration

No configuration is required for this module.

#### Notes

- Ensure that all file operations trigger a sync with DynamoDB.
- The default file tree is seeded on new user creation.
- Path resolution is handled by the `pathResolver` utility.
- Always check DynamoDB for the latest state on login to ensure persistence.

### .kiro/specs/03-terminal-app.md

#### Overview

# Module Overview

This file defines the specifications for a terminal application that runs inside a window and includes a command execution engine. It outlines the requirements, dependencies, classes, and functions necessary to build and operate the terminal app.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Zustand` | State management for terminal session data. |
| `FileSystemStore` | Interface for file system operations. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `Terminal` | Manages terminal UI and command execution. | `executeCommand(command: string)`, `updatePrompt(path: string)` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `parseInput` | `input: string` | `ParsedCommand` | Splits user input into command and arguments. |
| `executeCommand` | `command: string`, `args: string[]` | `string` | Executes the given command using `FileSystemStore`. |

#### Configuration

No specific configuration is required for this module.

#### Notes

- Ensure terminal state (current working directory, command history) is correctly persisted and retrieved using Zustand.
- Supported commands must be implemented to interact with `FileSystemStore`.
- Error handling for invalid commands should be robust and user-friendly.
- The terminal UI should support dark mode with a green/white monospace text theme.
- The cursor should be visually distinct and functional, with blinking capability.

### .kiro/specs/04-code-editor-app.md

#### Overview

# Module Overview

This file defines the specifications for the Monaco-based code editor app, which includes a multi-tab interface with syntax highlighting and auto-save functionality. It integrates with Zustand for state management and fileSystemStore for file operations.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `@monaco-editor/react` | Provides the Monaco Editor component for syntax highlighting. |
| `Zustand` | State management library to manage active tab state. |
| `fileSystemStore` | Manages file read/write operations. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `CodeEditor` | Main component for the Monaco editor. | `renderEditor`, `handleFileOpen`, `handleFileSave` |
| `TabBar` | Manages the tab interface. | `addTab`, `closeTab`, `selectTab` |
| `FileSidebar` | Displays the file tree and allows file opening. | `openFile`, `renderFileList` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `openTab` | `fileContent`, `fileName` | `void` | Adds a new tab with given file content and name. |
| `closeTab` | `tabId` | `void` | Closes the tab identified by `tabId`. |
| `addTab` | `fileContent`, `fileName` | `void` | Adds a new tab and returns the tab ID. |
| `debounceWriteFile` | `content`, `fileName` | `void` | Debounces file writing to `fileSystemStore` after 2 seconds. |

#### Configuration

- **Monaco Themes**: Configured for dark mode.
- **Auto-save Debounce**: Auto-saves file changes every 2 seconds.

#### Notes

- Ensure tabs are properly managed and closed to avoid memory leaks.
- The file tree sidebar is read-only and used to open files in the editor.
- Active tab state is persisted in Zustand to maintain user session continuity.
- Syntax highlighting is configured for `.js`, `.ts`, `.py`, `.json`, `.md`, `.txt` file types.

### .kiro/specs/05-file-explorer-app.md

#### Overview

# Module Overview

This file outlines the specifications for the File Explorer App, a visual file browser featuring a tree view, context menus, and file operations. It details the requirements, tasks, and acceptance criteria for implementing this feature.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `lucide-react` | Provides icons for different file types and folders. |
| `fileSystemStore` | Manages the state of the file system and handles file operations. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `TreeNode` | Represents a node in the file system tree view. | `render()`, `expand()`, `collapse()` |
| `ContextMenu` | Manages the context menu for file operations. | `createMenu()`, `handleAction()` |
| `Breadcrumb` | Displays breadcrumb navigation for the current path. | `renderBreadcrumb()` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `openInEditor` | `filePath` | `void` | Opens the file in the Code Editor. |
| `handleContextMenu` | `event`, `filePath` | `void` | Handles context menu actions. |

#### Configuration

No configuration settings are specified in this file.

#### Notes

- Ensure the tree view accurately reflects the current state of the file system.
- Context menu actions must integrate seamlessly with the `fileSystemStore`.
- Double-clicking a file should reliably open it in the Code Editor.
- Breadcrumb navigation must accurately represent the current path and allow for easy navigation.

### .kiro/specs/06-notes-app.md

#### Overview

# Module Overview

This file outlines the specifications for the Notes App, a simple rich-text note-taking application that supports markdown input and preview. It includes requirements, tasks, and acceptance criteria to guide the development of the app.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `marked` | Markdown parser for rendering markdown content |
| `fileSystemStore` | Manages file operations for saving and loading notes |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `NotesEditor` | Handles markdown input and updates preview | `updatePreview(content: string)` |
| `NotesPreview` | Displays the rendered markdown preview | `renderMarkdown(content: string)` |
| `NotesList` | Lists all saved notes | `loadNotes()`, `deleteNote(noteId: string)` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `createFile` | `filePath: string`, `content: string` | `void` | Saves markdown content to a file |
| `readFile` | `filePath: string` | `string` | Reads content from a file |

#### Configuration

No specific configuration is required for this module.

#### Notes

- Ensure the markdown parser is correctly configured to handle all markdown syntax.
- The file system store should handle file creation and reading atomically to avoid data corruption.
- The preview pane should update in real-time as the user types in the editor.
- The saved notes should be listed in chronological order with timestamps visible.

### .kiro/specs/07-browser-app.md

#### Overview

# Module Overview

This file defines the specifications for the embedded iframe browser component in the WebOS project. It includes the requirements, tasks, and acceptance criteria for implementing a basic browser app with a URL bar and navigation controls.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `react` | Core library for building UI components |
| `react-dom` | Library for rendering React components to the DOM |
| `styled-components` | Library for writing CSS in JS for styling components |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `Browser` | Main browser component | `renderIframe()`, `handleUrlChange()` |
| `AddressBar` | Component for URL input | `updateUrl()`, `handleSubmit()` |
| `NavigationControls` | Component for navigation buttons | `handleBack()`, `handleForward()` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `loadUrl` | `url: string` | `void` | Loads the given URL in the iframe |
| `handleRefresh` | `none` | `void` | Refreshes the current URL in the iframe |
| `showError` | `errorMessage: string` | `void` | Displays an error message for load failures |

#### Configuration

- **Default Homepage**: `about:blank` or a custom WebOS welcome page
- **CORS Limitations**: Acknowledged; some external sites may not load due to CORS policies

#### Notes

- Ensure to handle CORS errors gracefully and display user-friendly messages.
- The navigation controls are currently disabled for version 1 but have placeholders in the UI.
- The address bar should update its state on navigation changes, if possible.

### .kiro/specs/08-auth-cognito.md

#### Overview

# Module Overview

This file defines the specifications for implementing user authentication using AWS Cognito. It outlines the requirements, tasks, and acceptance criteria for integrating Cognito into our application, ensuring secure login, signup, and JWT token management.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `AWS` | Provides AWS SDK for Cognito interactions. |
| `Zustand` | State management library used for managing authentication state. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `authStore` | Manages user and token state. | `login`, `logout`, `signup` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `initiateAuth` | `username`, `password`, `client_id` | `AuthenticationResult` | Initiates user authentication with Cognito. |
| `verifyToken` | `token` | `boolean` | Verifies the validity of a JWT token. |

#### Configuration

| Configuration | Purpose |
| --- | --- |
| `Cognito User Pool ID` | Identifier for the Cognito User Pool. |
| `Client ID` | Application client ID for Cognito. |

#### Notes

- Ensure JWT tokens are stored in memory and not in `localStorage` to avoid security risks.
- Implement auto-refresh logic to renew tokens before expiry.
- All API routes must validate JWT tokens via middleware to ensure secure access.
- On successful login, load user-specific data from DynamoDB and seed defaults if the user is new.
- Error handling for invalid credentials should be clear and user-friendly.

### .kiro/specs/09-dynamo-persistence.md

#### Overview

# Module Overview

This file defines the backend integration with DynamoDB for persisting user data. It sets up the AWS SDK, provides service methods for CRUD operations, and implements routes for syncing various user data types.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `AWS SDK v3` | For interacting with DynamoDB |
| `dynamoClient.ts` | Sets up the DynamoDB client |
| `dynamoService.ts` | Provides CRUD operations for DynamoDB |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `DynamoClient` | Manages connection to DynamoDB | `connect()`, `disconnect()` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `createDynamoClient` | `config` | `DynamoClient` | Initializes DynamoDB client |
| `get` | `tableName`, `key` | `Item` | Retrieves an item from DynamoDB |
| `put` | `tableName`, `item` | `void` | Adds or updates an item in DynamoDB |
| `delete` | `tableName`, `key` | `void` | Deletes an item from DynamoDB |
| `batchWrite` | `requests` | `void` | Performs batch write operations |
| `query` | `tableName`, `indexName`, `keyConditionExpression` | `Items` | Retrieves items based on a query |

#### Configuration

| Key | Type | Description |
| --- | --- | --- |
| `DYNAMO_ENDPOINT` | `string` | DynamoDB endpoint URL |
| `DYNAMO_REGION` | `string` | AWS region for DynamoDB |

#### Notes

- Ensure all error handling is robust, especially for batch operations.
- The `batchWrite` method should handle retries for failed operations.
- Always validate input before performing any write operations to prevent corrupt data.
- The `query` function assumes consistent reads; adjust if high latency is expected.

### .kiro/specs/10-ai-assistant.md

#### Overview

# Module Overview

This file outlines the specifications for the AI Assistant feature, which provides a context-aware chat panel powered by the Claude API through a FastAPI microservice. The goal is to offer a seamless, interactive experience for users to get help with coding, debugging, and general queries.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `fastapi` | Framework for building the microservice. |
| `claude_api` | Client for interacting with the Claude API. |
| `sse` | Support for Server-Sent Events for real-time streaming responses. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `AIAssistant` | Manages interactions with the Claude API and chat UI. | `send_message`, `get_context`, `stream_response` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `get_open_file_context` | `current_file` | `dict` | Retrieves context about the current open file. |
| `get_terminal_context` | `cwd`, `recent_commands` | `dict` | Retrieves context about the current terminal state. |
| `stream_chat_response` | `messages`, `context` | `SSE stream` | Streams the response from the Claude API in real time. |

#### Configuration

| Setting | Purpose |
| --- | --- |
| `API_KEY` | API key for accessing the Claude service. |
| `CHAT_ROUTE` | Endpoint for the chat service. |

#### Notes

- Ensure that context data is updated before sending a request to the Claude API.
- The `stream_chat_response` function should handle any exceptions that arise during streaming.
- The chat UI component should handle real-time updates gracefully without flickering or losing context.

### .kiro/specs/11-advanced-features.md

#### Overview

# Module Overview

This file, `11-advanced-features.md`, outlines the advanced features and requirements for enhancing the production readiness of our application. It details the necessary components, functions, and configurations to implement sophisticated functionalities like settings management, keyboard shortcuts, window snapping, notifications, and performance optimizations.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `react` | Core React library for component creation and state management. |
| `react-dom` | Library for rendering React components to the DOM. |
| `react-window` | Library for efficiently rendering large lists and grids. |
| `redux` | State management library for managing application-wide state. |
| `styled-components` | Library for writing CSS styles in JavaScript. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `Settings` | Manages user settings like theme, wallpaper, and accent color. | `saveSettings()`, `loadSettings()` |
| `NotificationManager` | Handles notification creation and dismissal. | `createNotification()`, `dismissNotification()` |
| `WindowFrame` | Manages window snapping and resizing. | `snapWindow()`, `resizeWindow()` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `registerShortcut` | `key`, `callback` | `void` | Registers a global keyboard shortcut. |
| `showToast` | `message`, `duration` | `void` | Displays a toast notification. |
| `lazyLoadComponent` | `component` | `React.lazy()` | Returns a lazily loaded React component. |
| `optimizeFileTree` | `fileList` | `optimizedList` | Optimizes rendering of large file lists. |

#### Configuration

| Config | Purpose |
| --- | --- |
| `themeSettings` | Defines default and available themes. |
| `shortcutKeys` | Maps keyboard shortcuts to their respective callbacks. |
| `notificationSettings` | Configures toast notification behavior. |

#### Notes

- Ensure all keyboard shortcuts are registered in the `useEffect` hook to ensure global accessibility.
- Notification messages should auto-dismiss after a set duration to avoid clutter.
- Performance optimizations like lazy loading and virtualization should be rigorously tested to ensure no lag with large datasets.
- Error boundaries should be added around critical components to prevent app crashes from affecting the entire application.
- Window snapping and resizing should be smooth and responsive to provide a seamless user experience.

### .kiro/steering/architecture.md

#### Overview

# Module Overview

This file outlines the architectural guidelines and rules for the **kiro** project's steering module. It defines how different components interact, the dependencies they rely on, and the key classes and functions that facilitate these interactions.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `express` | Provides the backend framework for handling AWS operations. |
| `zustand` | Manages the UI state and global application state. |
| `fastapi` | Used for the AI service, which is a separate process called by the backend. |
| `framer-motion` | Handles all enter/exit animations for components. |
| `typescript` | Enforces strict typing across the project. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `AppRegistry` | Registers and manages self-contained React components. | `registerApp(app: React.FC)` |
| `WindowManager` | Manages the lifecycle of application windows. | `openWindow(windowId: string)`, `closeWindow(windowId: string)` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `fetchFromDynamo` | `query: string`, `params: object` | `Promise<object>` | Fetches data from DynamoDB via the backend. |
| `callAIService` | `request: object` | `Promise<object>` | Calls the AI service using FastAPI. |

#### Configuration

No specific configuration settings are defined in this file. Architectural rules are high-level guidelines rather than configurable options.

#### Constants

No constants are defined in this file.

#### Notes

- Ensure that the frontend never calls DynamoDB or Cognito directly; all AWS operations must go through the Express backend.
- Zustand is the single source of truth for UI state, and the file system is managed between Zustand (runtime) and DynamoDB (persistence).
- Each app is a self-contained React component registered in `AppRegistry`.
- Windows are managed by a central `WindowManager` Zustand slice.
- The AI service is a separate FastAPI process called by the backend only.
- Use TypeScript strict mode and avoid `any` types.
- Tailwind is used exclusively for styling, with no inline styles or CSS modules.
- Framer Motion is used for all enter/exit animations.

### .kiro/steering/dynamo-schema.md

#### Overview

# **kiro/steering/dynamo-schema.md**

#### Module Overview

This file defines the schema for our DynamoDB table `webos-main`. It outlines the table's structure, access patterns, and a global secondary index (GSI) to facilitate efficient data retrieval.

#### Dependencies

- **None**

#### Classes

| Class | Purpose | Key Methods |
|-------|---------|-------------|
| N/A   | N/A     | N/A         |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| N/A      | N/A        | N/A     | N/A         |

#### Configuration

| Key                | Value                  | Description                                      |
|--------------------|-------------------------|--------------------------------------------------|
| Table Name         | `webos-main`            | Name of the DynamoDB table.                      |
| Billing Mode       | `On-demand`             | Billing mode for the DynamoDB table.             |
| Region             | `us-east-1`             | AWS region where the DynamoDB table is deployed. |

#### Key Structure

| PK (Partition Key)            | SK (Sort Key)           | Description                                    |
|------------------------------|-------------------------|------------------------------------------------|
| `string → userId`            | `entity type + path`    | Primary key structure combining user ID and entity type. |

#### Access Patterns

| PK         | SK                              | Data                        |
|------------|---------------------------------|-----------------------------|
| `user#<id>`| `PROFILE`                       | `name, email, avatar, plan` |
| `user#<id>`| `SETTINGS`                      | `theme, wallpaper, accent` |
| `user#<id>`| `DESKTOP`                       | `iconPositions, lastSession`|
| `user#<id>`| `FS#/home/`                     | `type:folder, meta`         |
| `user#<id>`| `FS#/home/documents/`           | `type:folder, meta`         |
| `user#<id>`| `FS#/home/documents/notes.txt`  | `type:file, content, size`  |
| `user#<id>`| `APP#terminal`                  | `history[], cwd`            |
| `user#<id>`| `APP#editor`                    | `openTabs[], activeTab`     |

#### GSI (Global Secondary Index)

| Key                | Description                                      |
|--------------------|--------------------------------------------------|
| `GSI1: SK → PK`    | Allows reverse lookup for admin queries.          |

#### Notes

- Ensure that the `PK` and `SK` adhere to the specified format to maintain consistency.
- The `GSI1` is crucial for admin-related queries, so ensure it's properly maintained.
- The `SK` values for files include metadata like `type` and `size` to facilitate easy retrieval and management.

### .kiro/steering/product.md

#### Overview

# Module Overview

This file provides an overview of the `WebOS` browser-based Operating System simulation. It describes the system's architecture, key features, and its target users. This documentation is crucial for understanding the broader context of the project and how it integrates with other components.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `React` | Core library for building the user interface |
| `Framer Motion` | For smooth animations and transitions |
| `AWS DynamoDB` | For user data persistence |
| `AWS Cognito` | For user authentication and management |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `App` | Main application component | `render()` |
| `Terminal` | Terminal emulator component | `executeCommand()` |
| `AIAssistant` | AI assistant component | `query()` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `initializeApp` | `userID` | `Promise<void>` | Initializes the application with user data |
| `saveUserData` | `userData` | `Promise<void>` | Saves user data to DynamoDB |

#### Configuration

| Configuration | Purpose |
| --- | --- |
| `AWS_DYNAMODB_TABLE` | DynamoDB table name for user data |
| `AWS_COGNITO_USER_POOL` | Cognito user pool ID |

#### Notes

- Ensure that all user interactions are logged for debugging purposes.
- The system targets desktop browsers with a minimum width of 1280px.
- The dark-first style and glassmorphism theme are integral to the user experience.
- Animations are handled by Framer Motion and should be optimized for performance.

### .kiro/steering/tech-stack.md

#### Overview

# **kiro/steering/tech-stack.md**

#### Module Overview

This file details the technology stack used across the entire project, outlining the frontend, backend, AI service, and deployment platforms. It serves as a quick reference for developers to understand the tech choices and how they integrate.

#### Dependencies

| Dependency | Purpose |
|------------|---------|
| React 18 + Vite + TypeScript | Frontend framework and build tool |
| Tailwind CSS | Utility-first CSS framework for styling |
| Framer Motion | Animation library for React |
| Zustand | State management library |
| react-rnd | Library for draggable and resizable windows |
| Monaco Editor | Embedded code editor |
| @aws-sdk/client-cognito-identity-provider | AWS SDK for Cognito authentication |
| Node.js + Express + TypeScript | Backend framework and server |
| @aws-sdk/lib-dynamodb | AWS SDK for DynamoDB |
| Python 3.11 + FastAPI | AI service framework |
| Anthropic Claude API | AI service for natural language processing |
| Vercel | Frontend deployment platform |
| Railway | Backend and AI service deployment platform |
| AWS DynamoDB | Managed NoSQL database |
| AWS Cognito User Pool | User authentication service |

#### Classes

| Class | Purpose | Key Methods |
|-------|---------|-------------|
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| N/A | N/A | N/A | N/A |

#### Configuration

| Configuration | Purpose |
|----------------|---------|
| Environment variables | Manage sensitive configurations like API keys and endpoints |

#### Notes

- Ensure to follow the coding standards for both frontend (TypeScript) and backend (TypeScript) to maintain consistency.
- The frontend relies on Vercel for deployment, while the backend and AI service use Railway.
- AWS services are managed through the AWS SDK, with Cognito handling authentication and DynamoDB serving as the database.
- The AI service uses the Anthropic Claude API for natural language processing tasks.
- Always check the documentation for each service to understand the latest features and best practices.

### ai-service/.env.example

#### Overview

# ai-service/.env.example

This file contains example environment variables for configuring the AI service. It's used to demonstrate the required environment variables without exposing sensitive information.

#### Dependencies

No direct dependencies are listed here, but the service will typically require access to an external API, specifically the one referenced by `OPENAI_API_KEY`.

#### Configuration

| Variable        | Purpose                  | Example Value |
|-----------------|--------------------------|---------------|
| `OPENAI_API_KEY` | API key for accessing the external service | `sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx` |
| `PORT`           | Port number for the service to run on | `8000` |

#### Notes

- `OPENAI_API_KEY` should be replaced with a valid API key from the external service provider.
- The `PORT` value can be changed to any available port number as per the deployment requirements.

### ai-service/Procfile

#### Overview

# Module Overview

The `ai-service/Procfile` defines the command that triggers the web service when the application is deployed in a containerized environment like Heroku. It specifies that the `web` process should run the `main.py` script using Python.

# Dependencies

This file does not import any Python modules directly. It relies on the `main.py` script to handle the web service logic.

# Functions

There are no functions defined in this file.

# Classes

There are no classes defined in this file.

# Notes

- Ensure `main.py` is correctly set up to handle web requests.
- This file assumes Python is installed in the environment where the application is deployed.
- Any changes to `main.py` will affect the behavior of the web service.

### ai-service/README.md

#### Overview

# ai-service/README.md

#### Module Overview

This file serves as the primary documentation for the AI service module, detailing setup, usage, and configuration of the AI chat functionality integrated with WebOS using the OpenAI API.

#### Dependencies

| Import | Purpose |
|--------|---------|
| `openai` | Interacts with the OpenAI API to generate responses. |
| `flask` | Provides the web framework for creating the API endpoints. |
| `os` | Handles environment variables and file paths. |
| `dotenv` | Loads environment variables from a `.env` file. |

#### Classes

| Class | Purpose | Key Methods |
|-------|---------|-------------|
| `AIService` | Manages the AI chat functionality. | `__init__`, `chat`, `health_check` |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `health_check` | None | JSON | Returns the health status of the service. |
| `chat` | `message`, `context` | SSE stream | Initiates a chat session with the AI and streams the response. |

#### Configuration

- **Environment Variables**: Store the `OPENAI_API_KEY` and `PORT` in the `.env` file.
- **Models**: The default model is `gpt-3.5-turbo`. You can change it in `main.py`.

#### Notes

- Ensure the `.env` file is correctly configured to avoid "Invalid API key" errors.
- Monitor your usage to avoid hitting the rate limit; consider adding a payment method in the OpenAI dashboard.
- Costs are generally low, but keep an eye on your usage to prevent unexpected charges.

### ai-service/main.py

#### Overview

# ai-service/main.py

#### Module Overview

This file sets up a FastAPI application that provides an AI-powered chat service. It allows users to send messages and receive responses from an AI model, with context-aware prompts based on the user's current environment.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `fastapi` | Framework for building the API |
| `fastapi.middleware.cors` | Middleware for handling CORS |
| `fastapi.responses` | To create streaming responses |
| `pydantic` | For data validation using models |
| `openai` | To interact with the AI model |
| `os` | To access environment variables |
| `dotenv` | To load environment variables from a `.env` file |
| `json` | To handle JSON data |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `ChatRequest` | Validates and structures chat request data | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `health` | None | JSON | Returns the health status of the service |
| `chat` | `request: ChatRequest` | StreamingResponse | Handles chat requests and streams responses from the AI model |

#### Configuration

- `OPENAI_API_KEY`: Your API key for the OpenAI service.
- `PORT`: The port on which the FastAPI server will run (default is 8000).

#### Notes

- The `ChatRequest` class uses Pydantic to validate the incoming request data.
- The `/chat` endpoint streams responses from the AI model using `StreamingResponse`.
- The `generate` function inside the `chat` endpoint builds a context-aware prompt and handles exceptions.
- Make sure to have a `.env` file with the `OPENAI_API_KEY` set up to run this service.

### ai-service/requirements.txt

#### Overview

# Module Overview

This file, `ai-service/requirements.txt`, lists the Python dependencies required to run the AI service. It ensures that all necessary packages are installed and up-to-date for development and production environments.

#### Dependencies

| Dependency | Purpose | Key Features |
|------------|---------|-------------|
| fastapi==0.115.6 | A modern, fast (high-performance), web framework for building APIs with Python 3.8+ based on standard Python type hints. | Asynchronous, easy routing, automatic interactive API documentation. |
| uvicorn[standard]==0.34.0 | An ASGI server implementation for Python. | Asynchronous, high performance, easy to use. |
| openai==1.58.1 | Official Python SDK for the OpenAPI API. | Interface to interact with the OpenAPI service. |
| python-dotenv==1.0.1 | Loads environment variables from a.env file into `os.environ`. | Manages environment variables securely. |
| aiohttp==3.11.11 | A fast, concurrent HTTP client and server for asyncio. | Asynchronous HTTP client/server, supports WebSockets. |

#### Classes

| Class | Purpose | Key Methods |
|-------|---------|------------|
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| N/A | N/A | N/A | N/A |

#### Configuration

| Variable | Purpose | Example Value |
|----------|---------|---------------|
| N/A | N/A | N/A |

#### Notes

- Ensure that all dependencies are compatible with the current Python version used in the project.
- Regularly update dependencies to the latest stable versions to benefit from the latest features and security patches.
- Use environment variables to manage sensitive information such as API keys securely.

### backend/.env.example

#### Overview

# Backend `.env.example` Module Overview

This file defines environment variables used for configuring the backend services. It serves as a template for the `.env` file, which holds sensitive and environment-specific settings required by the application.

#### Dependencies

No direct dependencies are listed here, but the file is intended to be used in conjunction with the backend application which relies on these environment variables for configuration.

#### Configuration

| Variable        | Purpose                  | Example Value                  |
|-----------------|--------------------------|-------------------------------|
| `PORT`          | Port number for the server| `4000`                        |
| `AWS_REGION`    | AWS region for services  | `ap-south-1`                  |
| `DYNAMODB_TABLE`| DynamoDB table name      | `webos-main`                  |
| `COGNITO_USER_POOL_ID` | Cognito User Pool ID | `ap-south-1_xxxxxxxxx`        |
| `COGNITO_CLIENT_ID` | Cognito Client ID | `xxxxxxxxxxxxxxxxxxxxxxxxxx` |
| `AI_SERVICE_URL` | URL for AI service        | `http://localhost:8000`       |
| `NODE_ENV`      | Environment (development, production) | `development` |

#### Notes

- This file is a template and should be copied to `.env` before use.
- Ensure sensitive information like `COGNITO_USER_POOL_ID` and `COGNITO_CLIENT_ID` is securely managed and not hard-coded.
- The `AI_SERVICE_URL` is set to a local URL, which should be updated for production environments.

### backend/Procfile

#### Overview

# Backend Module Overview

The `backend/Procfile` defines the commands that are run by the Heroku platform to start the application's processes. This file is essential for deploying the application on Heroku, ensuring that the web server starts correctly.

#### Dependencies

No dependencies are listed here as this file is a simple configuration file.

#### Functions

No functions are defined in this file.

#### Classes

No classes are defined in this file.

#### Configuration

| Configuration | Purpose |
|---------------|---------|
| `web: npm start` | Starts the web server using the `npm start` command defined in `package.json`. |

#### Notes

- Ensure that the `start` script in `package.json` is correctly configured to start the web server.
- This file is crucial for deploying the application on Heroku and should not be modified unless necessary.

### backend/package.json

#### Module Overview

The `backend/package.json` file defines the dependencies and scripts for our backend project. It ensures that all necessary packages are installed and provides commands to run the development server, build TypeScript files, and start the application.

#### Dependencies

| Package | Purpose |
| --- | --- |
| `express` | A web framework for Node.js to handle HTTP requests and responses. |
| `@aws-sdk/lib-dynamodb` | AWS SDK library for interacting with DynamoDB. |
| `@aws-sdk/client-dynamodb` | AWS SDK client for DynamoDB operations. |
| `aws-jwt-verify` | Library for verifying JWT tokens from AWS. |
| `dotenv` | Loads environment variables from a `.env` file. |
| `cors` | Middleware for enabling CORS with various options. |
| `axios` | Promise-based HTTP client for making requests. |

#### Scripts

| Script | Purpose |
| --- | --- |
| `dev` | Runs the TypeScript server in watch mode. |
| `build` | Compiles TypeScript files to JavaScript. |
| `start` | Starts the application using the compiled JavaScript. |

#### Notes

- Ensure environment variables are set in the `.env` file before running the development server.
- The `build` script should be run before starting the application to ensure all TypeScript files are compiled.
- The `aws-jwt-verify` package is crucial for validating JWT tokens from AWS services.

### backend/tsconfig.json

#### Overview

# Backend `tsconfig.json`

This file configures the TypeScript compiler for the backend part of the project. It sets compiler options that dictate how TypeScript code is compiled into JavaScript.

#### Dependencies

No direct dependencies are listed here, but it relies on TypeScript to compile the TypeScript files in the `src` directory into JavaScript files in the `dist` directory.

#### Configuration

| Option            | Purpose                                                                                     | Details                                                                                           |
|-------------------|---------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------|
| `target`          | Specifies the ECMAScript target version                                                      | `ES2022`                                                                                        |
| `module`          | Defines the module code generation                                                          | `ESNext`                                                                                        |
| `lib`             | Includes library files to be generated in the compilation output                             | `["ES2022"]`                                                                                     |
| `moduleResolution`| Specifies the module resolution strategy                                                     | `node`                                                                                           |
| `esModuleInterop`  | Enables interoperability between CommonJS and ES Modules                                      | `true`                                                                                           |
| `strict`          | Enables all strict type-checking options                                                     | `true`                                                                                           |
| `skipLibCheck`    | Skips type checking of all declaration files (`.d.ts`)                                      | `true`                                                                                           |
| `outDir`          | Redirects output structure                                                                  | `./dist`                                                                                        |
| `rootDir`         | Specifies the root directory of input files                                                  | `./src`                                                                                          |
| `resolveJsonModule`| Allows importing `.json` files directly as modules                                           | `true`                                                                                           |
| `forceConsistentCasingInFileNames`| Ensures that file names are consistently cased in imports and exports                        | `true`                                                                                           |

#### Notes

- The `strict` option ensures that type checking is thorough, which helps catch potential bugs early.
- `skipLibCheck` is enabled to speed up compilation since we trust the type definitions provided by TypeScript libraries.
- `resolveJsonModule` is enabled to simplify importing JSON files directly.
- The `outDir` and `rootDir` options ensure that compiled files are placed in the correct directories.

### backend/src/server.ts

#### Overview

# Backend Server Documentation

#### Module Overview

This file sets up and runs the backend server for the application. It initializes the Express app, configures middleware, sets up routes, and starts the server listening on a specified port.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `express` | Web framework for building the server and handling HTTP requests. |
| `cors` | Middleware to handle Cross-Origin Resource Sharing. |
| `dotenv` | Loads environment variables from a `.env` file. |
| `authRoutes` | Import of authentication routes. |
| `filesRoutes` | Import of file management routes. |
| `aiRoutes` | Import of AI-related routes. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `app` | Express application instance. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `dotenv.config()` | None | None | Loads environment variables from `.env` file. |
| `app.listen(PORT, callback)` | `PORT` | None | Starts the server and listens on the specified port. |

#### Configuration

- `PORT`: Environment variable for the server port, defaults to `4000` if not set.

#### Notes

- The server uses CORS middleware to allow requests from any origin.
- The server has a health check endpoint at `/health`.
- Environment variables should be set in a `.env` file in the root directory.
- The server logs a message when it starts up, indicating the port it's running on.

### backend/src/middleware/authMiddleware.ts

#### Overview

# Backend Middleware: `authMiddleware.ts`

#### Module Overview

This file defines the `authMiddleware` function, which verifies JWT tokens from AWS Cognito and attaches the user ID to the request object. This middleware is essential for securing routes that require authenticated access.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Request`, `Response`, `NextFunction` | Types from Express to handle HTTP requests and responses. |
| `CognitoJwtVerifier` | A library to verify JWT tokens issued by AWS Cognito. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `CognitoJwtVerifier` | Verifies JWT tokens from AWS Cognito. | `verify(token: string): Promise<any>` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `authMiddleware` | `req: AuthRequest`, `res: Response`, `next: NextFunction` | `Promise<void>` | Verifies the JWT token and sets the `userId` on the request object. |

#### Configuration or Constants

| Constant | Purpose |
| --- | --- |
| `verifier` | An instance of `CognitoJwtVerifier` configured with Cognito details. |

#### Notes

- Ensure environment variables `COGNITO_USER_POOL_ID` and `COGNITO_CLIENT_ID` are set for token verification.
- If the token is missing or invalid, the middleware responds with a 401 status and an error message.
- The `userId` is extracted from the token's `sub` claim and attached to the request object for downstream use.

### backend/src/routes/ai.ts

#### Overview

# Backend Module Documentation: `backend/src/routes/ai.ts`

#### Module Overview

This file defines the Express routes for handling AI-related requests, specifically for chat interactions. It uses middleware for authentication and proxies requests to an AI service.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Router` | Express router instance for defining routes. |
| `axios` | HTTP client for making requests to the AI service. |
| `authMiddleware, AuthRequest` | Middleware and request type for authentication. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `router.post('/chat', authMiddleware, async (req: AuthRequest, res) => {... })` | `req: AuthRequest`, `res` | `void` | Handles POST requests for AI chat, proxies to the AI service, and streams the response back to the client. |

#### Configuration or Constants

| Constant | Purpose |
| --- | --- |
| `AI_SERVICE_URL` | URL for the AI service, defaults to `http://localhost:8000` if not set in environment variables. |

#### Notes

- The route `/chat` expects a JSON body with `message` and `context`.
- The response from the AI service is streamed back to the client using Server-Sent Events (SSE).
- Errors are logged and a 500 status is returned if the AI service is unavailable.
- Ensure environment variable `AI_SERVICE_URL` is set for production deployments.

### backend/src/routes/auth.ts

#### Overview

# backend/src/routes/auth.ts

#### Module Overview

This file defines the authentication routes for the application, handling user login, signup, and profile retrieval. It uses AWS Cognito for user management and authentication.

#### Dependencies

| Import | Description |
| --- | --- |
| `Router` from `express` | Express Router instance to define routes |
| `CognitoIdentityProviderClient`, `InitiateAuthCommand`, `SignUpCommand` from `@aws-sdk/client-cognito-identity-provider` | AWS SDK clients and commands for interacting with Cognito |
| `authMiddleware` from `../middleware/authMiddleware.js` | Middleware to protect routes and provide user context |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `router.post('/login',...)` | `req`, `res` | JSON with token and user info or error | Handles user login using Cognito |
| `router.post('/signup',...)` | `req`, `res` | JSON with token and user info or error | Handles user signup and auto-login using Cognito |
| `router.get('/me',...)` | `req`, `res` | JSON with user info | Retrieves user profile, protected by auth middleware |

#### Notes

- The `id` in the user object is currently extracted from the token, which is not secure. In production, use a library to decode the token and extract the user ID.
- The email and name in the `/me` route are placeholders. In a real scenario, these should be fetched from the user's profile stored in Cognito.
- Error handling is basic; consider adding more robust error responses and logging.

### backend/src/routes/files.ts

#### Overview

### Module Overview

The `backend/src/routes/files.ts` file defines the Express routes for file-related operations. It handles requests for retrieving, creating, deleting, and updating file nodes in a user's file system.

### Dependencies

| Import | Purpose |
| --- | --- |
| `Router` from `express` | To create and manage routes |
| `authMiddleware` and `AuthRequest` from `../middleware/authMiddleware.js` | To authenticate requests and provide user ID |
| `getUserFileSystem`, `saveFileNode`, `deleteFileNode` from `../services/dynamoService.js` | To interact with the DynamoDB file system |
| `FSNode` from `../types/index.js` | To define the structure of file nodes |

### Functions

| Route | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `router.get('/', authMiddleware,...)` | N/A | JSON representation of user's file system | Retrieves the user's file system |
| `router.post('/', authMiddleware,...)` | `path`, `type`, `content` | JSON `{ success: true }` | Creates a new file or folder |
| `router.delete('/', authMiddleware,...)` | `path` (in query) | JSON `{ success: true }` | Deletes a file or folder |
| `router.patch('/', authMiddleware,...)` | `path`, `content` | JSON `{ success: true }` | Updates the content of a file |

### Notes

- The `router.patch` route currently updates the entire file content without checking for existing nodes. In production, implement a proper node lookup and update mechanism.
- Error handling logs errors to the console and responds with a 500 status code for server errors.
- The `path` parameter in the `router.delete` route is expected in the query string, not in the request body.

### backend/src/services/dynamoService.ts

#### Overview

# backend/src/services/dynamoService.ts

#### Module Overview

This file provides a service layer for interacting with a DynamoDB database. It abstracts the AWS SDK's DynamoDB operations, allowing the application to manage user file systems in a structured and efficient manner.

#### Dependencies

| Import | Purpose |
|--------|---------|
| `DynamoDBClient` | AWS SDK client for DynamoDB |
| `DynamoDBDocumentClient` | Simplified client for DynamoDB operations |
| `GetCommand`, `PutCommand`, `QueryCommand`, `DeleteCommand` | Commands for specific DynamoDB operations |
| `FSNode` | Type definition for file system nodes |

#### Classes

| Class | Purpose | Key Methods |
|-------|---------|-------------|
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `getUserFileSystem` | `userId: string` | `Promise<FSNode | null>` | Fetches and reconstructs a user's file system from DynamoDB |
| `saveFileNode` | `userId: string`, `node: FSNode` | `Promise<void>` | Saves a file node to DynamoDB |
| `deleteFileNode` | `userId: string`, `path: string` | `Promise<void>` | Deletes a file node from DynamoDB |
| `createDefaultFileSystem` | N/A | `FSNode` | Creates a default file system structure |
| `reconstructFileTree` | `items: any[]` | `FSNode` | Reconstructs a file tree from a flat list of items |

#### Configuration

| Variable | Default | Description |
|----------|---------|-------------|
| `TABLE_NAME` | `'webos-main'` | DynamoDB table name |

#### Notes

- Ensure environment variables `AWS_REGION` and `DYNAMODB_TABLE` are set.
- Error handling is minimal; consider adding more robust error management in production.
- The `createDefaultFileSystem` function provides a fallback for users with no existing data.
- The `reconstructFileTree` function assumes a flat list of items and builds a hierarchical tree.

### backend/src/types/index.ts

#### Overview

# Backend Module Overview

The `backend/src/types/index.ts` file defines TypeScript interfaces for key data structures used throughout the backend codebase. These interfaces ensure type safety and consistency when working with filesystem nodes, user information, and authenticated requests.

#### Dependencies

This file relies on the following imports:
- `Express.Request`: Provides the base structure for Express requests, extended to include custom properties.

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Interfaces

| Interface | Purpose | Description |
| --- | --- | --- |
| `FSNode` | Filesystem Node | Represents a file or folder in the filesystem, including metadata like creation and modification times. |
| `User` | User Information | Stores user details such as ID, email, name, and optional avatar. |
| `AuthRequest` | Extended Express Request | Extends the Express `Request` object to include an optional `userId` for authenticated requests. |

#### Notes

- The `FSNode` interface includes optional properties (`content`, `size`, `children`, `avatar`), which may not always be present.
- The `AuthRequest` interface extends the `Express.Request` to include a `userId` property, facilitating user identification in authenticated routes.

### frontend/.env.example

#### Overview

# frontend/.env.example

This file contains environment variable placeholders used to configure the frontend application. It serves as a template for developers to create their own `.env` file with actual values for different environments.

#### Dependencies

No direct dependencies are listed here as it's a configuration file.

#### Configuration

| Variable | Purpose |
| --- | --- |
| `VITE_API_URL` | Base URL for the backend API. |
| `VITE_COGNITO_USER_POOL_ID` | Cognito User Pool ID for authentication. |
| `VITE_COGNITO_CLIENT_ID` | Cognito Client ID for authentication. |
| `VITE_COGNITO_REGION` | AWS region for Cognito services. |

#### Notes

- Developers must create a `.env` file in the `frontend` directory and populate it with actual values from their environment.
- This file is not committed to version control to protect sensitive information.
- Ensure the `.env` file is included in `.gitignore` to avoid accidental commits.

### frontend/index.html

#### Overview

# frontend/index.html

This file is the entry point for the frontend application. It sets up the basic HTML structure and includes the main JavaScript module that bootstraps the application.

#### Dependencies

- **favicon.svg**: The application icon used in the browser tab.
- **/src/main.tsx**: The main JavaScript module that initializes the React application.

#### Classes

| Class | Purpose | Key Methods |
|-------|---------|-------------|
| None  | N/A     | N/A         |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|----------|-------------|
| None     | N/A        | N/A      | N/A         |

#### Configuration or Constants

- **meta charset**: Sets the character encoding for the HTML document to UTF-8.
- **meta viewport**: Ensures the page is responsive on all devices by setting the viewport to the device width and initial scale to 1.0.
- **link rel="icon"**: Links the application icon (favicon) for use in browser tabs and bookmarks.

#### Notes

- The `<div id="app"></div>` element is where the React application will be mounted.
- The `<script type="module" src="/src/main.tsx"></script>` tag imports the main JavaScript module that handles the application logic and rendering.

### frontend/package.json

#### Overview

# frontend/package.json

This file defines the dependencies and scripts for the frontend application. It ensures that all necessary packages are installed and outlines how to run various development tasks.

#### Dependencies

| Package | Purpose |
| --- | --- |
| `react` | Core library for building user interfaces. |
| `react-dom` | Library for rendering React components to the DOM. |
| `zustand` | State management library. |
| `framer-motion` | Animation library for React. |
| `react-rnd` | Library for draggable and resizable React components. |
| `@monaco-editor/react` | React wrapper for the Monaco Editor. |
| `@aws-sdk/client-cognito-identity-provider` | AWS SDK for interacting with Cognito Identity Provider. |
| `lucide-react` | Icon library. |
| `marked` | Markdown parser and compiler. |
| `axios` | Promise-based HTTP client for making API requests. |

#### DevDependencies

| Package | Purpose |
| --- | --- |
| `@types/react` | TypeScript definitions for React. |
| `@types/react-dom` | TypeScript definitions for ReactDOM. |
| `@vitejs/plugin-react` | Vite plugin for React. |
| `typescript` | Typed superset of JavaScript. |
| `vite` | Next-generation build tool. |
| `tailwindcss` | Utility-first CSS framework. |
| `postcss` | CSS post-processing framework. |
| `autoprefixer` | PostCSS plugin for adding vendor prefixes. |

#### Scripts

| Script | Purpose |
| --- | --- |
| `dev` | Starts the development server using Vite. |
| `build` | Compiles TypeScript and builds the project with Vite. |
| `preview` | Generates static files for preview. |

#### Notes

- Ensure `npm install` is run to install dependencies before starting the development server.
- The `build` script compiles TypeScript and then runs the production build with Vite.
- The `preview` script is useful for generating static files for deployment previews.

### frontend/postcss.config.js

#### Overview

# frontend/postcss.config.js

This file configures PostCSS plugins for our frontend project. It sets up TailwindCSS and Autoprefixer to handle CSS styling and compatibility across different browsers.

#### Dependencies

- `tailwindcss`: Integrates TailwindCSS for utility-first CSS framework.
- `autoprefixer`: Automatically adds vendor prefixes to CSS rules using values from `caniuse-lite`.

#### Configuration

| Configuration | Purpose |
|---------------|---------|
| `tailwindcss` | Enables TailwindCSS for utility-first CSS. |
| `autoprefixer` | Adds necessary vendor prefixes to CSS. |

#### Notes

- Ensure that TailwindCSS and Autoprefixer are installed as dependencies in `package.json`.
- This configuration assumes that PostCSS is already set up in the project.

### frontend/tailwind.config.js

#### Overview

# frontend/tailwind.config.js

This file configures Tailwind CSS for our project. It defines the scope of files to scan for class names, enables dark mode, extends the default theme with custom colors, backdrop blurs, animations, and keyframes, and includes any necessary plugins.

#### Dependencies

- **tailwindcss**: The utility-first CSS framework for rapidly building custom user interfaces.

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `export default` | None | Tailwind CSS configuration object | Default export of the configuration object for Tailwind CSS |

#### Configuration

| Key | Value | Description |
| --- | --- | --- |
| `content` | ['./index.html', './src/**/*.{js,ts,jsx,tsx}'] | Files and directories to scan for classes |
| `darkMode` | 'class' | Enables dark mode based on a class |
| `theme.extend` | Custom values | Extends the default Tailwind CSS theme |
| `colors.glass` | Custom colors | Defines custom glass colors for light and dark modes |
| `backdropBlur` | Custom backdrop blurs | Adds custom backdrop blur values |
| `animation` | Custom animations | Adds custom animations like fade-in and slide-up |
| `keyframes` | Custom keyframes | Defines custom keyframes for animations like fadeIn and slideUp |

#### Notes

- Ensure that the `darkMode` is consistently applied by adding the `dark` class to the root element in your HTML.
- Custom colors and animations should be referenced correctly in your components to see the intended effects.
- Tailwind CSS configuration can be extended further as needed by adding to the `theme.extend` section.

### frontend/tsconfig.json

#### Overview

# frontend/tsconfig.json

This file configures the TypeScript compiler for our frontend codebase. It sets compiler options to ensure type safety and compatibility with modern JavaScript features.

#### Dependencies

- **TypeScript**: The primary dependency, used for type-checking and compiling our TypeScript code to JavaScript.

#### Configuration

| Setting | Purpose | Details |
| --- | --- | --- |
| `target` | Sets the JavaScript version | `es2023` |
| `module` | Defines module code generation | `esnext` |
| `lib` | Lists libraries to include | `["ES2023", "DOM", "DOM.Iterable"]` |
| `types` | Adds type definitions | `["vite/client"]` |
| `skipLibCheck` | Skips type checking of all declaration files | `true` |
| `moduleResolution` | Specifies module resolution strategy | `bundler` |
| `allowImportingTsExtensions` | Allows importing TS extensions | `true` |
| `verbatimModuleSyntax` | Uses original module syntax | `true` |
| `moduleDetection` | Forces module detection | `force` |
| `noEmit` | Prevents emitting output files | `true` |
| `strict` | Enables all strict type-checking options | `true` |
| `noUnusedLocals` | Reports errors on unused locals | `true` |
| `noUnusedParameters` | Reports errors on unused parameters | `true` |
| `erasableSyntaxOnly` | Allows only erasable syntax | `true` |
| `noFallthroughCasesInSwitch` | Disallows fallthrough cases in switch | `true` |
| `jsx` | Specifies JSX code transformation | `react-jsx` |

#### Notes

- The `noEmit` option is set to `true` to leverage bundlers like Vite for compilation and bundling.
- The `strict` setting ensures that TypeScript enforces strict type-checking rules.
- The `jsx` option is configured for React's JSX transformation.

### frontend/vercel.json

#### Overview

# frontend/vercel.json

This file configures the deployment settings for our frontend application on Vercel. It specifies the build command, output directory, framework, and URL rewrites to ensure the app functions correctly in the Vercel environment.

#### Dependencies

No direct dependencies are imported in this file.

#### Configuration

| Key            | Purpose                                                                                     |
|----------------|---------------------------------------------------------------------------------------------|
| `buildCommand` | Specifies the command to run for building the project (`npm run build`).                    |
| `outputDirectory` | Defines the directory where the build output will be placed (`dist`).                     |
| `framework` | Indicates the framework used (`vite`).                                                        |
| `rewrites` | An array of rewrite rules to handle routing. |

#### Functions

No functions are defined in this file.

#### Notes

- Ensure `npm run build` is correctly configured in your `package.json` to build the project.
- The `rewrites` rule ensures all routes are redirected to `index.html` for client-side routing.

### frontend/vite.config.ts

#### Overview

# frontend/vite.config.ts

This file configures the Vite build tool for our React-based frontend application. It sets up plugins and server options, including a proxy for API requests.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `defineConfig` | A function from `vite` to define the configuration object. |
| `react` | A plugin from `@vitejs/plugin-react` to enable React support in Vite. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `defineConfig` | `config` | Configuration object | Returns a configuration object for Vite. |

#### Configuration

| Setting | Purpose |
| --- | --- |
| `plugins` | Specifies plugins to use in the build process. Here, it includes the `react` plugin. |
| `server.port` | Sets the port on which the development server runs. Default is `3000`. |
| `server.proxy` | Configures a proxy to forward API requests to a specified target URL. |

#### Notes

- The `proxy` configuration uses an environment variable `VITE_API_URL` to determine the target API URL. If not set, it defaults to `http://localhost:4000`.
- Ensure environment variables are correctly set in your `.env` file for the proxy to work as expected.

### frontend/src/App.tsx

#### Overview

# frontend/src/App.tsx

#### Module Overview

This file defines the main entry point for our React application. It manages the initial authentication check and renders either the `LoginScreen` or `Desktop` component based on the user's authentication status.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `useEffect` | Hook to perform side effects in function components. |
| `useAuthStore` | Custom hook to access authentication state and methods. |
| `LoginScreen` | Component for the login interface. |
| `Desktop` | Component for the main application interface. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `App` | Main application component. | None |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `checkAuth` | None | None | Checks the user's authentication status. |

#### Notes

- The `useEffect` hook triggers `checkAuth` to verify the user's authentication status when the component mounts.
- If `isLoading` is `true`, a loading screen is displayed.
- Once authentication is checked, the component renders either `LoginScreen` or `Desktop` based on the user's authentication status.

### frontend/src/main.tsx

#### Overview

# frontend/src/main.tsx

This file initializes the React application. It sets up the root React component and renders it to the DOM.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `React` | Core React library for building UI components. |
| `ReactDOM` | Provides DOM-specific methods for rendering and unmounting React components. |
| `App` | Main application component. |
| `./style.css` | Global styles for the application. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `ReactDOM.createRoot` | `HTMLElement` | `ReactRoot` | Returns the root React component used for rendering. |

#### Notes

- The `React.StrictMode` wrapper is used to highlight potential problems in the application.
- Ensure the DOM element with the id `app` exists before this script runs to avoid runtime errors.

### frontend/src/style.css

#### Overview

# Module Overview

The `frontend/src/style.css` file is the primary stylesheet for our frontend application. It sets up global styles, normalizes default browser styling, and defines reusable CSS classes for consistent typography, layout, and component appearance.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `@tailwind base;` | Tailwind CSS base styles for consistent spacing and typography. |
| `@tailwind components;` | Tailwind CSS component styles for buttons, forms, etc. |
| `@tailwind utilities;` | Tailwind CSS utility classes for margin, padding, colors, etc. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `.prose` | Styles for markdown prose, including headings, paragraphs, and code blocks. | N/A |
| `.prose h1`, `.prose h2`, `.prose p`, `.prose ul`, `.prose ol`, `.prose code`, `.prose pre` | Specific styles for various elements within `.prose`. | N/A |

#### Notes

- The `*` selector resets margin, padding, and box-sizing for all elements to ensure consistency.
- The `body` styles ensure a clean, readable font stack and smooth text rendering.
- The `#app` styles make the main application container take up the full viewport.
- The `.prose` class and its nested elements are designed for rendering markdown content with a consistent look and feel.
- When adding new styles, consider if they should be added to `.prose` or if they are more specific to individual components.

### frontend/src/components/AppLauncher.tsx

#### Overview

# frontend/src/components/AppLauncher.tsx

#### Module Overview

`AppLauncher.tsx` renders a list of application launcher buttons. Each button corresponds to an application registered in `appRegistry` and triggers a window opening when clicked.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `appRegistry` | Provides the list of applications to be launched. |
| `useWindowStore` | Hook to access the `openWindow` function for launching applications. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `AppLauncher` | N/A | JSX element | Renders application launcher buttons based on `appRegistry`. |

#### Configuration

| Identifier | Purpose |
| --- | --- |
| `appRegistry` | Array of application objects, each with `id`, `name`, and `icon`. |

#### Notes

- The `openWindow` function is called with the `app.id` and `app.name` when a button is clicked.
- Each button has a title displaying the application name for accessibility.
- The button's icon is rendered using the `app.icon` property.
- The button's styling includes hover effects for better user interaction feedback.

### frontend/src/components/Desktop.tsx

#### Overview

# frontend/src/components/Desktop.tsx

#### Module Overview

This file defines the `Desktop` component, which is the main visual container for the desktop interface. It integrates various UI elements like the `WindowManager`, `Taskbar`, and `AppLauncher`, and manages the theme and authentication states.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `useEffect` | React hook to perform side effects in function components. |
| `useThemeStore` | Context store for theme-related data. |
| `useAuthStore` | Context store for authentication-related data. |
| `useFileSystemStore` | Context store for file system operations. |
| `WindowManager` | Component managing window display and interactions. |
| `Taskbar` | Component displaying the taskbar UI. |
| `AppLauncher` | Component for launching applications. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `Desktop` | Main container for the desktop UI. | None |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `Desktop` | None | JSX Element | Renders the desktop layout with integrated components. |

#### Configuration or Constants

None

#### Notes

- The `useEffect` hook loads the file system when the user is authenticated.
- The `background` style of the `div` is set dynamically based on the `wallpaper` from the theme store.
- The `mode` determines if the `dark` class is applied to the container.

### frontend/src/components/LoginScreen.tsx

#### Overview

# Module Overview

The `frontend/src/components/LoginScreen.tsx` file defines the login and signup interface for the application. It provides a form for users to enter their credentials and toggle between login and signup modes.

#### Dependencies

| Import | Purpose |
|--------|---------|
| `useState` | React hook for managing component state. |
| `useAuthStore` | Custom hook to access authentication methods. |
| `motion` | From `framer-motion`, used for animated transitions. |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `handleSubmit` | `e: React.FormEvent` | `void` | Handles form submission, either logging in or signing up the user. |

#### Notes

- The `handleSubmit` function toggles between login and signup based on the `isLogin` state.
- The form fields dynamically adjust based on whether the user is logging in or signing up.
- Error messages are displayed if the login or signup process fails.
- The component uses a gradient background and animations for a polished user experience.

### frontend/src/components/Taskbar.tsx

#### Overview

# Taskbar Component Documentation

The `Taskbar.tsx` file defines the `Taskbar` component, which is a UI element that displays open windows and system information such as the current time and user name. It also includes a logout button.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Clock`, `LogOut` from `lucide-react` | Icons for the time and logout button |
| `useAuthStore` from `../stores/authStore` | Provides authentication state and logout function |
| `useWindowStore` from `../stores/windowStore` | Provides window management state and focus function |
| `useState`, `useEffect` from `react` | React hooks for state management and side effects |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `Taskbar` | None | JSX for the Taskbar component | Renders the Taskbar UI with open windows, time, and user information |

#### Notes

- The `useEffect` hook updates the time every second.
- The `windows` array from `useWindowStore` is mapped to create buttons for each open window.
- The `focusWindow` function from `useWindowStore` is called when a window button is clicked.
- The `logout` function from `useAuthStore` is called when the logout button is clicked.
- The `time` state is formatted using `toLocaleTimeString` to display the current time.

### frontend/src/components/WindowFrame.tsx

#### Overview

# WindowFrame.tsx

This file defines the `WindowFrame` component, which renders a draggable and resizable window for an application. It integrates with the application's state management to handle window actions like minimizing, maximizing, and closing.

#### Dependencies

| Import | Description |
| --- | --- |
| `Rnd` | A React component for draggable and resizable elements. |
| `motion` | A utility from `framer-motion` for animations. |
| `X`, `Minus`, `Maximize2`, `Minimize2` | Icons from `lucide-react` for window controls. |
| `useWindowStore` | A hook from the window store for managing window state. |
| `WindowState`, `AppDefinition` | Types for window and app definitions. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `WindowFrame` | Renders a window frame with draggable and resizable capabilities. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| N/A | N/A | N/A | N/A |

#### Configuration

| Property | Description |
| --- | --- |
| `minWidth`, `minHeight` | Minimum dimensions for the window, falling back to 400px and 300px if not specified. |
| `bounds` | Sets the boundaries for dragging and resizing to the parent element. |
| `dragHandleClassName` | Class name for the drag handle area of the window. |
| `style` | Dynamic styles applied to the window, including `zIndex`. |
| `disableDragging` | Disables dragging when the window is maximized. |
| `enableResizing` | Enables resizing unless the window is maximized. |

#### Notes

- The component returns `null` if the window is minimized, effectively hiding it.
- The window's content is rendered using the `Component` passed from the `app` prop.
- Animations are handled by `framer-motion` to provide smooth transitions for window appearance and disappearance.
- Ensure that the `useWindowStore` hook is properly set up in your application context to manage window states.

## Code Improvements Applied

- Added regression tests for high-risk files:
  - `frontend/src/types/index.ts`
  - `frontend/src/stores/windowStore.ts`
  - `frontend/src/stores/fileSystemStore.ts`
- Proposed extraction of shared utility code into a dedicated module.
- Updated documentation with regenerated walkthroughs and diagrams.
